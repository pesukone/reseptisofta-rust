[package]
name = "recipes"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
librecipe = { path = "../librecipe", version = "0.1.0" }
fraction = "0.10.0"
gtk = { version = "0.4.7", package = "gtk4" }
